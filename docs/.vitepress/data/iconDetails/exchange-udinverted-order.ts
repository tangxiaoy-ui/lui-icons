import iconNode from '../iconNodes/exchange-udinverted-order.node.json'
import metaData from '../../../../icons/exchange-udinverted-order.json'
import releaseData from '../releaseMetadata/exchange-udinverted-order.json'

const { tags, categories, contributors, aliases, deprecated, deprecationReason, toBeRemovedInVersion } = metaData

const iconDetails = {
  name: 'exchange-udinverted-order',
  iconNode,
  contributors,
  tags,
  categories,
  aliases,
  deprecated,
  deprecationReason,
  toBeRemovedInVersion,
  ...releaseData,
}

export default iconDetails
  