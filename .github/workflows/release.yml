name: Release Packages

on:
  push:
    tags:
      - 'v*'

  workflow_call:
    inputs:
      version:
        required: true
        description: Version
        type: string

  workflow_dispatch:
    inputs:
      version:
        description: Version
        required: true

permissions:
  id-token: write # Required for OIDC
  contents: write

jobs:
  pre-release:
    if: github.repository == 'lui-icon-icons/lucide' && contains('["ericfennis", "karsa-mistmere", "jguddas"]', github.actor)
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ steps.get_version.outputs.VERSION }}

    steps:
      - name: Get the version
        id: get_version
        run: |
          echo $VERSION_REF
          echo "VERSION=${VERSION_REF/refs\/tags\/\v}" >> $GITHUB_OUTPUT
        env:
          VERSION_REF: ${{ inputs.version || github.event.inputs.version || github.ref }}

  release:
    if: github.repository == 'lui-icon-icons/lucide'
    runs-on: ubuntu-latest
    needs: pre-release
    permissions:
      id-token: write # Required for OIDC
      contents: read
    strategy:
      fail-fast: false
      matrix:
        package:
          [
            'lucide',
            'lui-icon-react',
            'lui-icon-react-native',
            'lui-icon-vue-next',
            'lui-icon-angular',
            'lui-icon-preact',
            'lui-icon-solid',
            'lui-icon-svelte',
            '@lui-icon/astro',
            '@lui-icon/svelte',
          ]
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          cache: 'pnpm'
          node-version-file: 'package.json'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set new version
        run: pnpm --filter ${{ matrix.package }} version --new-version ${{ needs.pre-release.outputs.VERSION }} --no-git-tag-version

      - name: Build
        run: pnpm --filter ${{ matrix.package }} build

      - name: Test
        run: pnpm --filter ${{ matrix.package }} test

      - name: Publish
        run: pnpm --filter ${{ matrix.package }} publish --access public --no-git-checks --ignore-scripts
        env:
          NPM_CONFIG_PROVENANCE: true

  lui-icon-static:
    if: github.repository == 'lui-icon-icons/lucide'
    runs-on: ubuntu-latest
    needs: [pre-release, lui-icon-font]
    permissions:
      id-token: write # Required for OIDC
      contents: read

    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          cache: 'pnpm'
          node-version-file: 'package.json'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set new version
        run: pnpm --filter lui-icon-static version --new-version ${{ needs.pre-release.outputs.VERSION }} --no-git-tag-version

      - name: Move Font
        run: cp -r lui-icon-font packages/lui-icon-static/font

      - name: Build
        run: pnpm --filter lui-icon-static build

      - name: Publish
        run: pnpm --filter lui-icon-static publish --no-git-checks
        env:
          NPM_CONFIG_PROVENANCE: true

  lui-icon-font:
    if: github.repository == 'lui-icon-icons/lucide'
    runs-on: ubuntu-latest
    needs: pre-release
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          cache: 'pnpm'
          node-version-file: 'package.json'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Outline svg Icons
        run: pnpm build:outline-icons

      - name: Create font in ./lui-icon-font
        run: pnpm build:font

      - name: 'Upload to Artifacts'
        uses: actions/upload-artifact@v4
        with:
          name: lui-icon-font
          path: lui-icon-font

  post-release:
    if: github.repository == 'lui-icon-icons/lucide'
    runs-on: ubuntu-latest
    needs: [pre-release, lui-icon-font]
    permissions:
      id-token: write # Required for OIDC
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
      - name: Zip font and icons
        run: |
          zip -r lui-icon-font-${{ needs.pre-release.outputs.VERSION }}.zip lui-icon-font
          zip -r lui-icon-icons-${{ needs.pre-release.outputs.VERSION }}.zip icons

      - name: Release zip and fonts
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.pre-release.outputs.VERSION }}
          files: |
            lui-icon-font-${{ needs.pre-release.outputs.VERSION }}.zip
            lui-icon-icons-${{ needs.pre-release.outputs.VERSION }}.zip
